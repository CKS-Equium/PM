---
slug: <kebab-case-slug>
repo: https://github.com/<org>/<slug>
status: proposed          # proposed → active → paused → shipped → archived
phase: discovery          # discovery → design → build → test → review → release → post-mortem
created: <YYYY-MM-DD>
team: [orchestrator, product-manager]   # roles currently assigned/active
board: <github project url>             # optional
---

## Summary

One paragraph: what this project is and why it exists.

## Current state

Orchestrator-maintained snapshot: active milestone, current phase, blockers, the next gate and
its status. Updated as the project moves.

## Decision log

- <YYYY-MM-DD> — <decision> because <reason>.

## Gate waivers

- <YYYY-MM-DD> — gate <#> waived because <reason>. *(only if a gate was consciously skipped)*
